\section{Implementation}

\subsection{Implementation of the RAM}

\subsubsection{Interface}
The module operates on a single clock signal (\texttt{clk}). The interface definition is detailed in Table \ref{tab:interface}. The figure \ref{fig:ram_dual_port} illustrates the block schematic of the True Dual-Port RAM.

\begin{figure}
    \centering
    \includegraphics[width=0.7\textwidth]{images/RAM_schematic.pdf}
    \caption{True Dual-Port RAM Block Schematic}
    \label{fig:ram_dual_port}
\end{figure}

\begin{table}
    \centering
    \caption{RAM Interface Signals}
    \label{tab:interface}
    \begin{tabular}{@{}llll@{}}
        \toprule
        \textbf{Port Name} & \textbf{Direction} & \textbf{Width} & \textbf{Description} \\ 
        \midrule
        \texttt{clk} & Input & 1 & System clock (rising edge triggered). \\
        \midrule
        \multicolumn{4}{l}{\textit{Port A}} \\
        \texttt{data\_a} & Input & \texttt{DATA\_WIDTH} & Data to be written to memory. \\
        \texttt{addr\_a} & Input & \texttt{ADDR\_WIDTH} & Address for read/write operations. \\
        \texttt{we\_a} & Input & 1 & Write Enable (High = Write, Low = Read). \\
        \texttt{q\_a} & Output & \texttt{DATA\_WIDTH} & Data read from memory. \\
        \midrule
        \multicolumn{4}{l}{\textit{Port B}} \\
        \texttt{data\_b} & Input & \texttt{DATA\_WIDTH} & Data to be written to memory. \\
        \texttt{addr\_b} & Input & \texttt{ADDR\_WIDTH} & Address for read/write operations. \\
        \texttt{we\_b} & Input & 1 & Write Enable (High = Write, Low = Read). \\
        \texttt{q\_b} & Output & \texttt{DATA\_WIDTH} & Data read from memory. \\
        \bottomrule
    \end{tabular}
\end{table}

\subsubsection{Functional Behavior}
The RAM operates under the following conditions:
\begin{itemize}
    \item \textbf{Synchronous Operation:} All memory transactions occur on the rising edge of \texttt{clk}.
    \item \textbf{Dual Access:} Port A and Port B operate independently and can be active simultaneously.
    \item \textbf{Read/Write Modes:}
    \begin{itemize}
        \item \textbf{Write Mode (\texttt{we = 1}):} The input data is written to the specified address.
        \item \textbf{Read Mode (\texttt{we = 0}):} The data at the specified address is registered into the output \texttt{q}.
    \end{itemize}
\end{itemize}
However, simultaneous read and write operations to the same address from different ports will lead to undefined behavior.

\subsubsection{Storage Structure}
The memory core is modeled abstractly as a two-dimensional array of registers. This structure is sized dynamically based on the \texttt{DATA\_WIDTH} and \texttt{MEM\_DEPTH} parameters. The model is specifically designed to allow synthesis tools to infer dedicated hardware Block RAM (BRAM) resources on FPGA targets, ensuring optimal area usage and timing performance compared to distributed logic storage.

\subsubsection{Parallel Control Logic}
To achieve True Dual-Port functionality, the design employs two distinct, parallel control processes triggered by the rising edge of the clock. Each process manages the operations of a single port (A or B) independently. Since both processes share access to the same underlying storage array, the component supports simultaneous Read/Read, Read/Write, and Write/Write operations across the two ports.

\subsubsection{Read/Write Arbitration Logic}
The internal logic for each port enforces a mutually exclusive operation mode based on the Write Enable (\texttt{we}) signal:

\begin{enumerate}
    \item \textbf{Write Priority:} When the write enable signal is active, the system prioritizes the storage operation. The data present on the input bus is written directly into the memory array at the specified address.
    
    \item \textbf{Output Behavior during Write:} The implementation utilizes a ``Read-Disabled'' (or No-Change) architecture during write cycles. This means that while data is being written to memory, the output data port is \textit{not} updated with the new input nor the old memory content. Instead, it retains the value held from the previous clock cycle.
    
    \item \textbf{Read Operation:} Only when the write enable signal is inactive does the system retrieve data from the memory array and update the output register.
\end{enumerate}

This logic structure minimizes the hardware overhead by avoiding complex ``Read-First'' or ``Write-First'' arbitration circuitry, making it highly efficient for standard buffer and storage applications.

\subsection{Twiddle Factor ROM Design and Implementation}
\begin{figure}
    \centering
    \includegraphics[width=0.7\textwidth]{images/rom_twiddle_arch.png}
    \caption{The architecture of ROM twiddle factor}
    \label{fig:rom_twiddle_arch}
\end{figure}


The twiddle factor ROM is designed to efficiently generate complex exponential coefficients required by FFT butterfly operations while minimizing memory usage.
Instead of storing the full set of $N$ twiddle factors, the proposed architecture stores sine and cosine values only for the first octant of the unit circle, corresponding to the angular range $[0,\pi/4)$.
All remaining twiddle factors are reconstructed using symmetry properties of trigonometric functions.

The twiddle factor is defined as
\begin{equation}
W_N^k = e^{-j\frac{2\pi k}{N}} = \cos(\beta) - j\sin(\beta),
\quad \beta = \frac{2\pi k}{N}.
\end{equation}

By exploiting symmetry, only $N/8$ base values are stored in ROM, leading to an eightfold reduction in memory size.
The complete twiddle factor reconstruction is achieved using address mirroring, sine--cosine swapping, and sign inversion.

\subsubsection{Symmetrical Mapping of Sine and Cosine}

Table~\ref{tab:twiddle_mapping} summarizes the symmetry rules used to reconstruct sine and cosine values over the full range $[0,2\pi)$ from the first-octant base interval.

\begin{table}
\centering
\caption{Symmetrical mapping of twiddle factors}
\label{tab:twiddle_mapping}
\begin{tabular}{c|c|c}
\toprule
Angle range $\beta$ & $\sin(\beta)$ & $\cos(\beta)$ \\
\midrule
$0 \sim \pi/4$ &
$-\sin(\beta)$ &
$\cos(\beta)$ \\

$\pi/4 \sim \pi/2$ &
$-\cos(\tfrac{\pi}{2}-\beta)$ &
$\sin(\tfrac{\pi}{2}-\beta)$ \\

$\pi/2 \sim 3\pi/4$ &
$-\cos(\beta-\tfrac{\pi}{2})$ &
$-\sin(\beta-\tfrac{\pi}{2})$ \\

$3\pi/4 \sim \pi$ &
$-\sin(\pi-\beta)$ &
$-\cos(\pi-\beta)$ \\

$\pi \sim 5\pi/4$ &
$\sin(\beta-\pi)$ &
$-\cos(\beta-\pi)$ \\

$5\pi/4 \sim 3\pi/2$ &
$\cos(\tfrac{3\pi}{2}-\beta)$ &
$-\sin(\tfrac{3\pi}{2}-\beta)$ \\

$3\pi/2 \sim 7\pi/4$ &
$\cos(\beta-\tfrac{3\pi}{2})$ &
$\sin(\beta-\tfrac{3\pi}{2})$ \\

$7\pi/4 \sim 2\pi$ &
$\sin(2\pi-\beta)$ &
$\cos(2\pi-\beta)$ \\
\bottomrule
\end{tabular}
\end{table}

The mapping rules in Table~\ref{tab:twiddle_mapping} form the theoretical basis of the ROM reconstruction logic and are directly reflected in the Verilog implementation.

\subsubsection{Module-Level Implementation Overview}

Fig.~\ref{fig:rom_twiddle_arch} illustrates the structural organization of the twiddle factor generator.
The design is composed of several lightweight combinational modules, each responsible for a specific reconstruction step:

\begin{itemize}
    \item \textbf{\texttt{mux\_addr} (Address Mapping Unit)}  
    This module maps the intra-octant offset to a valid ROM address.
    For odd octants, address mirroring is applied using
    \[
    \texttt{rom\_addr} = (N/8) - \texttt{off},
    \]
    ensuring that all accesses fall within the first-octant ROM range.
    This block is purely combinational and introduces no clock latency.

    \item \textbf{\texttt{rom\_twiddle} (Compact Sine/Cosine ROM)}  
    This module stores precomputed sine and cosine values for angles in $[0,\pi/4)$.
    The ROM is implemented as a combinational lookup table initialized from external data files.
    Once the address is stable, the corresponding sine and cosine values propagate directly to the output.

    \item \textbf{\texttt{Inv\_FP} (Sign Inversion Units)}  
    These modules generate negated sine and cosine values by flipping the IEEE-754 sign bit.
    This method avoids arithmetic subtraction and significantly reduces hardware complexity.
    The sign inversion logic is fully combinational.

    \item \textbf{\texttt{mux\_output} (Symmetry Reconstruction Unit)}  
    Based on the octant index, this module selects between sine and cosine base values,
    applies optional sign inversion, and performs sine--cosine swapping.
    The implemented selection logic directly follows the symmetry rules listed in
    Table~\ref{tab:twiddle_mapping}.

    \item \textbf{\texttt{rom\_twiddle\_top} (Top-Level Twiddle Generator)}  
    This top-level module integrates all submodules and produces the final complex twiddle factor.
    Boundary cases, where the offset equals zero, are handled using predefined constants
    (e.g., $\pm1$, $0$, $\pm\sqrt{2}/2$) to avoid unnecessary ROM access and improve numerical robustness.
\end{itemize}

Although all reconstruction logic is combinational, the generated twiddle factor is synchronized at the system level.
This ensures deterministic timing behavior and seamless integration with the pipelined FFT butterfly units.

Overall, the proposed ROM design achieves substantial memory reduction while maintaining exact trigonometric correctness,
making it well suited for high-performance FFT hardware implementations.

\subsection{Arithmetic Logic Unit (ALU)}

The Arithmetic Logic Unit (ALU) serves as the computational core of the system, designed to execute floating-point (FP) arithmetic operations with high throughput. The architecture is fully pipelined to optimize processing speed while maintaining a compact hardware footprint. The ALU design addresses three distinct operational scenarios: 
\begin{enumerate}
    \item Radix-2 Decimation-in-Frequency (DIF) Butterfly operation.
    \item Radix-2 Decimation-in-Time (DIT) Butterfly operation.
    \item Point-wise Multiplication (MPW) for spectral convolution.
\end{enumerate}

\begin{figure}
    \centering
    \includegraphics[width=0.9\textwidth]{images/ALU_Spec.png}
    \caption{Block Diagram of Arithmetic Logic Unit (ALU)}
\end{figure}

\subsubsection*{Data Path Control and Temporal Alignment}
To support the multi-mode functionality, the datapath employs a network of 64-bit multiplexers (MUX) to dynamically route operands based on the selected operation mode (controlled by signals \texttt{MUX\_BMU} and \texttt{MUX\_MPW}). A critical challenge in pipelined complex arithmetic is ensuring that operands arrive at the arithmetic units (Adders/Multipliers) at the exact same clock cycle. 

To address this, we integrated specific delay buffers (as illustrated in the block diagram) to achieve \textbf{temporal alignment}. These buffers compensate for the latency differences between parallel paths, ensuring the correctness of the algorithm and the integrity of the pipeline stages.

\subsubsection{Addition Floating Point Unit}

The proposed Floating-Point Adder architecture is compliant with the IEEE 754 single-precision standard and is implemented using a 5-stage pipeline to maximize throughput. The datapath is decomposed into discrete functional blocks, utilizing Sign-Magnitude representation for efficient internal processing. The operation of each pipeline stage is described as follows:

\begin{itemize}
    \item \textbf{Stage 1: Exponent Comparison (\texttt{AFP\_Compare}):} 
    This stage compares the exponents of the two operands ($E_A, E_B$). The absolute difference $\delta = |E_A - E_B|$ is calculated to determine the alignment shift amount. Simultaneously, the larger operand is identified to guide the datapath routing, and the hidden bit is appended to form 24-bit significands.

    \item \textbf{Stage 2: Mantissa Alignment (\texttt{AFP\_Shift}):} 
    To enable arithmetic operations, the significand of the smaller number is right-shifted by $\delta$ positions. This is implemented using a hardware \textit{Barrel Shifter}, ensuring that the binary points of both operands are aligned within a single clock cycle.

    \item \textbf{Stage 3: Arithmetic Operation (\texttt{AFP\_Add}):} 
    The core addition or subtraction is performed based on the effective operation sign. The ALU computes $M_{sum} = M_{large} \pm M_{shifted}$. The result is then converted back to Sign-Magnitude format (taking the absolute value if the result is negative) to prepare for normalization.

    \item \textbf{Stage 4: Normalization Detection (\texttt{AFP\_Normal}):} 
    To handle potential leading zeros resulting from subtraction, this stage employs a \textit{Priority Encoder} (Leading One Detector). It scans the arithmetic result to determine the necessary left-shift amount ($L_{shift}$) required to restore the significand to the normalized $1.xxxx$ format.

    \item \textbf{Stage 5: Normalization and Rounding (\texttt{AFP\_Rounding}):} 
    The final stage executes the normalization shift ($M_{final} = M_{sum} \ll L_{shift}$) and adjusts the exponent accordingly ($E_{final} = E_{large} - L_{shift}$). Finally, Round-to-Nearest logic is applied to the guard bits, and the data is packed into the 32-bit IEEE 754 output format.
\end{itemize}

\begin{figure}[ht]
    \centering
    \includegraphics[width=1.0\textwidth]{images/AddFP_Interface.png}
    \caption{5-Stage Pipelined Architecture of the Floating Point Adder}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.7\textwidth]{images/AddFPSpec.png}
    \caption{Architecture of the Floating Point Adder}
    \label{fig:fp_adder_arch}
\end{figure}

\pagebreak

\subsubsection{Multiplication Floating Point Unit}

To maintain high throughput and strictly adhere to the timing constraint of $T_{clk} \le 8\text{ns}$, the multiplier architecture avoids a single-cycle $24 \times 24$-bit operation. Instead, we implement a multi-stage pipeline based on a \textbf{4-bit Nibble Decomposition Strategy}. This approach distributes the computational load across 7 pipeline stages (6 for accumulation and 1 for normalization).

\textbf{Partial Product Accumulation (Stages 1-6):}
The core multiplication logic decomposes the 24-bit operand $B$ into six 4-bit nibbles. In each stage $i$ ($i=0..5$), a $24 \times 4$-bit multiplication is performed, and the result is accumulated with the shifted partial sum from the previous stage according to the recursive formula:
\begin{equation}
    P_i = (P_{i-1} \gg 4) + (M_A \times M_B[4i+3 : 4i])
\end{equation}
This design ensures that the combinational depth of each stage remains shallow, effectively fitting within the required clock period.

\textbf{Normalization and Final Packaging (Final Stage):}
The output of the accumulation pipeline is a 48-bit raw mantissa. The final stage executes post-processing operations to comply with the IEEE 754 standard:
\begin{itemize}
    \item \textbf{Normalization:} The leading non-zero bit is detected. If necessary, the mantissa is shifted, and the exponent is adjusted to ensure the format $1.xxxx$.
    \item \textbf{Rounding:} Round-to-Nearest logic is applied to truncate the result to 23 fraction bits, after which the Sign, Exponent, and Mantissa are packed into the final 32-bit register.
\end{itemize}

\subsubsection{Complex Adder Unit}

The Complex Adder Unit serves as a structural wrapper designed to perform addition on complex numbers according to the formula $(R_1 + jI_1) + (R_2 + jI_2) = (R_1+R_2) + j(I_1+I_2)$. To maximize throughput, the architecture exploits the inherent independence of the real and imaginary components.

\textbf{Data Organization and Routing:}
The system utilizes a 64-bit data bus to represent a complex number. The input signals are split into two distinct 32-bit channels:
\begin{itemize}
    \item \textbf{Imaginary Part ($I$):} Occupies the upper 32 bits ($[63:32]$).
    \item \textbf{Real Part ($R$):} Occupies the lower 32 bits ($[31:0]$).
\end{itemize}

\textbf{Parallel Processing:}
The hardware implementation instantiates two parallel Floating-Point Adder cores (as described in the previous section). These cores share the same system clock and control signals but process data independently. Consequently, the Real and Imaginary sums are computed simultaneously, ensuring that the latency of the Complex Adder remains identical to that of a single FP Adder (5 clock cycles).

\textbf{Subtraction Operation:} same as addition, but the second operand is negated bit 32 of the floating point before feeding into the FP Adders.

\subsubsection{Complex Multiplier Unit}

\textbf{Architectural Decision:}
To ensure pipeline regularity and simplify timing closure, the design adopts the \textbf{Standard Four-Multiplication Architecture} over the Gauss algorithm. This choice avoids the structural complexity of pre/post-adders inherent in the Gauss method, prioritizing a uniform datapath flow.

\textbf{Pipelined Implementation and Timing Guarantee:}
The unit performs complex multiplication $Z = (AC - BD) + j(AD + BC)$ by decomposing 64-bit inputs into 32-bit floating-point components. The architecture guarantees a fixed total latency of \textbf{12 clock cycles}, structured as follows:

\begin{itemize}
    \item \textbf{Phase 1 (Cycles 1--7):} Four pipelined Floating-Point Multipliers operate in parallel to generate the partial products ($AC, BD, AD, BC$).
    \item \textbf{Phase 2 (Cycles 8--12):} The partial products are immediately routed to parallel Floating-Point Adders/Subtractors to compute the final Real ($AC-BD$) and Imaginary ($AD+BC$) parts.
\end{itemize}

This strict \textbf{12-cycle deterministic latency} ensures synchronization with the global control unit, preventing data hazards in the FFT processing stages.

\subsubsection{Divider by N Unit}

The normalization step in the IFFT algorithm requires dividing the output by the FFT length $N$. Since the system parameter $N$ is constrained to be a power of 2 ($N = 2^k$), implementing a full floating-point divider is computationally redundant. Instead, we employ an optimized \textbf{Exponent Subtraction} technique.

\textbf{Operation Principle:}
Dividing a floating-point number $X$ by $2^k$ is equivalent to subtracting $k$ from its exponent component.
\begin{equation}
    E_{out} = E_{in} - \log_2(N)
\end{equation}
The Sign bit and Mantissa field are passed through to the output unchanged, significantly reducing logic resources compared to a standard divider.

\textbf{Exception Handling (Zero \& Underflow):}
To ensure numerical correctness, the unit includes logic to handle boundary cases. The output is forced to the standard IEEE 754 Zero format ($32'h00000000$) if:
\begin{itemize}
    \item The input value is Zero.
    \item The subtraction results in a negative exponent ($E_{in} < \log_2(N)$), indicating an underflow where the result is too small to be represented as a normalized number.
\end{itemize}

\subsubsection{System Integration and Latency Characterization}

Following the implementation and verification of individual functional modules, the final design phase focused on top-level integration and global routing. A critical objective was to establish a \textbf{Pipeline Synchronization Strategy}. By precisely characterizing the latency of each sub-module, we inserted appropriate delay buffers at the input stages to align data streams, ensuring that operands arrive at the arithmetic cores at the exact required clock cycle.

Based on the synthesized datapath, the total processing latency for each operational mode is determined as follows:

\begin{itemize}
    \item \textbf{FFT (DIF) and IFFT (DIT) Modes:} \textbf{17 Clock Cycles.} \\
    The Butterfly operation inherently requires the serial execution of a Complex Addition/Subtraction and a Complex Multiplication.
    \begin{equation*}
        \text{Latency}_{Butterfly} = \text{Lat}_{ComplexAdd} (5) + \text{Lat}_{ComplexMul} (12) = 17 \text{ cycles}
    \end{equation*}
    
    \item \textbf{Multi-Point Wise (MPW) Mode:} \textbf{13 Clock Cycles.} \\
    This operation consists of the spectral multiplication followed by the normalization (Divide by N) stage.
    \begin{equation*}
        \text{Latency}_{MPW} = \text{Lat}_{ComplexMul} (12) + \text{Lat}_{DivN} (1) = 13 \text{ cycles}
    \end{equation*}
\end{itemize}

\textbf{Conclusion:}
The global architecture successfully manages inter-stage latencies. By strictly controlling the delay constraints for DIF, DIT, and MPW modes, the system ensures a seamless, stall-free pipeline flow, guaranteeing data integrity throughout the convolution process.

\subsection{Control Unit Design}

The Control Unit (CU) serves as the central orchestrator, managing the system's operation through a deterministic Finite State Machine (FSM). It works in tandem with an Address Generation Unit (AGU) to supply precise memory pointers and strictly manages the pipeline latency to ensure data integrity.

\subsubsection{Finite State Machine (FSM) Architecture}
The system operation is modeled as a sequential state transition flow. Upon activation, the FSM progresses through the following lifecycle:
\begin{equation*}
    \texttt{S\_IDLE} \xrightarrow{\text{Start}} \texttt{S\_LOAD} \xrightarrow{} \underbrace{\texttt{S\_FFTX} \to \texttt{S\_FFTH}}_{\text{Forward Transforms}} \xrightarrow{} \texttt{S\_MPW} \xrightarrow{} \underbrace{\texttt{S\_IFFT}}_{\text{Inverse Transform}} \xrightarrow{} \texttt{S\_OUT} \to \texttt{S\_DONE}
\end{equation*}

This linear progression ensures that the memory is initialized, the convolution is computed in the frequency domain, and the results are reconstructed before the system signals completion.

\subsubsection{Address Generation Unit (AGU)}
To minimize hardware area, the AGU shares arithmetic logic between the forward and inverse transforms.

\textbf{1. Decimation-in-Frequency (DIF) Strategy:}
For the Forward FFT stages (\texttt{S\_FFTX}, \texttt{S\_FFTH}), the AGU calculates addresses based on the current \texttt{state} (representing the stage $s$) and \texttt{step} (butterfly index $k$). The addressing formulas are defined as:
\begin{align}
    \text{Distance} &= \frac{N}{2^{\text{state} + 1}} \\
    \text{Addr}_{\text{RAM1}} &= (\text{step} \% \text{Distance}) + \left( \frac{\text{step}}{\text{Distance}} \right) \times \text{Distance} \times 2 \\
    \text{Addr}_{\text{RAM2}} &= \text{Addr}_{\text{RAM1}} + \text{Distance} \\
    \text{Addr}_{\text{Twiddle}} &= (\text{step} \% \text{Distance}) \times \frac{N}{2 \times \text{Distance}}
\end{align}

\textbf{2. Decimation-in-Time (DIT) Strategy:}
For the Inverse FFT stage (\texttt{S\_IFFT}), the addressing logic utilizes the same hardware structure as the DIF strategy to maximize resource reuse. The key distinction lies in the traversal direction: while DIF increments the stage index from $0$ to $\log_2 N - 1$, the DIT process operates in \textbf{reverse order}, decrementing the \texttt{state} from $\log_2 N - 1$ down to $0$. This effectively inverts the butterfly hierarchy required for the inverse transform.

\subsubsection{Latency Calculation and Pipeline Scheduling}
To manage the pipeline depth effectively, the FSM organizes processing into discrete \textbf{computational batches}. The total system latency is deterministic and derived as follows:

\textbf{1. FFT/IFFT Processing (Batch-based Execution):}
Each transform stage is divided into batches of 16 Butterfly operations. A single batch consists of a \textit{Pipeline Fill} phase (17 cycles) and a \textit{Pipeline Drain} phase (16 cycles), totaling 33 cycles.
\begin{itemize}
    \item \textbf{Cycles per Stage:} With $N/32$ batches required per stage, the time to complete one stage is:
    \begin{equation}
        T_{\text{stage}} = \frac{N}{32} \times 33 \quad [\text{cycles}]
    \end{equation}
    \item \textbf{Total Transform Time:} For $\log_2 N$ stages, the latency for one full FFT/IFFT is:
    \begin{equation}
        T_{\text{transform}} = \frac{33N}{32} \log_2 N \quad [\text{cycles}]
    \end{equation}
\end{itemize}

\textbf{2. Spectral Multiplication (MPW) Latency:}
The MPW stage processes data in blocks of 8. Each block incurs a latency of 21 cycles ($13 \text{ Read} + 8 \text{ Write}$).
\begin{equation}
    T_{\text{MPW}} = \frac{N}{8} \times 21 = \frac{21N}{8} \quad [\text{cycles}]
\end{equation}

\textbf{3. Total System Latency:}
The total execution time from \texttt{start} to \texttt{done} sums the latencies of two Forward FFTs, one MPW, and one Inverse FFT:
\begin{equation}
    T_{\text{Total}} \approx 2 \times T_{\text{transform}} + T_{\text{MPW}} + T_{\text{transform}}
\end{equation}
Substituting the generalized formulas:
\begin{equation}
    T_{\text{Total}} \approx 3 \times \left( \frac{33N}{32} \log_2 N \right) + \frac{21N}{8}
\end{equation}
This formula demonstrates that the system performance scales linearly with $N \log N$, constrained primarily by the pipeline depth of the arithmetic units.

\section{Results Synthesis and Implementation}

The design was synthesized and implemented targeting the **Digilent Arty Z7-20** evaluation platform (Device: \texttt{xc7z020-clg400-1}) using the Xilinx Vivado Design Suite. The implementation configuration was optimized to leverage the specific resources of the Zynq-7000 SoC architecture. The following subsections detail the experimental results regarding hardware complexity, timing performance, and power consumption.
% ---------------------------------------------------------
% 1. HARDWARE COMPLEXITY
% ---------------------------------------------------------
\subsection{Hardware Complexity and Resource Utilization}

The post-implementation resource usage is summarized in Table \ref{tab:resource_util}. The design fits comfortably within the target Zynq-7000 device constraints.

\begin{table}[h]
\centering
\caption{Resource Utilization Summary (Device: xc7z020)}
\label{tab:resource_util}
\begin{tabular}{|l|r|r|l|}
\hline
\textbf{Resource Type} & \textbf{Used} & \textbf{Utilization (\%)} & \textbf{Note} \\ \hline
Slice LUTs             & 5,197         & 9.77\%                    & Logic \& Memory \\ \hline
Slice Registers        & 3,161         & 2.97\%                    & Pipeline FFs \\ \hline
Slices                 & 1,542         & 11.59\%                   & - \\ \hline
Block RAM Tile         & 2             & 1.43\%                    & FIFO \& Buffers \\ \hline
Bonded IOB             & 3             & 2.40\%                    & I/O Ports \\ \hline
BUFGCTRL               & 3             & 9.38\%                    & Global Clocking \\ \hline
\end{tabular}
\end{table}


\begin{figure}[h]
    \centering
    \includegraphics[width=1.0\textwidth]{images/Conv_Until.png}
    \caption{Convolution Utilization Overview}
\end{figure}

\textbf{Analysis:}
\begin{itemize}
    \item \textbf{High Efficiency:} The design occupies less than 10\% of the available Logic resources (LUTs). This low footprint validates the architectural decision to time-multiplex the ALU for FFT, MPW, and IFFT operations, effectively saving silicon area.
    \item \textbf{Memory Optimization:} Only 1.43\% of Block RAM is utilized. The design relies primarily on distributed RAM (LUT as Memory: 197 units) for small buffers, preventing BRAM exhaustion and leaving resources available for other system components.
    \item \textbf{Pipeline Overhead:} The utilization of Slice Registers (~3\%) is proportional to the pipeline depth (5-7 stages), confirming that the pipelining strategy did not incur excessive area penalties.
\end{itemize}

% ---------------------------------------------------------
% 2. TIMING PERFORMANCE
% ---------------------------------------------------------
\subsection{Timing Analysis and Performance}

The system was constrained to a target clock period of $8.0\text{ns}$ ($125 \text{MHz}$). The timing results in Table \ref{tab:timing_res} confirm that all constraints were met.

\begin{table}[h]
\centering
\caption{Timing Performance Summary (Target: 8.000 ns)}
\label{tab:timing_res}
\begin{tabular}{|l|r|l|}
\hline
\textbf{Parameter} & \textbf{Value} & \textbf{Unit} \\ \hline
Worst Negative Slack (WNS) & \textbf{+0.265} & ns \\ \hline
Total Critical Path Delay & 7.607 & ns \\ \hline
\quad - Logic Delay & 4.157 & ns \\ \hline
\quad - Net (Routing) Delay & 3.450 & ns \\ \hline
Logic Levels & 9 & Levels \\ \hline
High Fanout & 102 & Nets \\ \hline
\end{tabular}
\end{table}

\begin{figure}[h]
    \centering
    \includegraphics[width=1.0\textwidth]{images/Conv_Time.png}
    \caption{Convolution Timing Overview}
\end{figure}

\textbf{Analysis:}
\begin{itemize}
    \item \textbf{Frequency Goal Met:} With a positive WNS of $+0.265\text{ns}$, the design operates reliably at $125\text{MHz}$.
    \item \textbf{Critical Path:} The worst-case path is located in the \textit{Complex Multiplier} stage (Delay: 7.607ns). The delay distribution is balanced between Logic (~55\%) and Routing (~45\%), indicating a healthy placement without severe congestion.
    \item \textbf{Pipeline Depth:} The critical path spans 9 logic levels. This confirms that the chosen pipeline depth was necessary; reducing the pipeline stages would likely have increased logic levels per cycle, violating the 8ns constraint.
\end{itemize}

% ---------------------------------------------------------
% 3. POWER CONSUMPTION
% ---------------------------------------------------------
\subsection{Power Consumption Profile}

The power analysis results, derived from the implemented netlist under typical process conditions ($25^\circ\text{C}$ ambient), are detailed in Table \ref{tab:power_res}.

\begin{table}[h]
\centering
\caption{On-Chip Power Consumption Analysis}
\label{tab:power_res}
\begin{tabular}{|l|r|r|}
\hline
\textbf{Power Component} & \textbf{Value (W)} & \textbf{Percentage} \\ \hline
\textbf{Total On-Chip Power} & \textbf{0.164} & \textbf{100\%} \\ \hline
\hline
Device Static Power & 0.104 & 63\% \\ \hline
Dynamic Power & 0.060 & 37\% \\ \hline
\quad - Clocks & 0.024 & (40\% of Dynamic) \\ \hline
\quad - Signals & 0.013 & (22\% of Dynamic) \\ \hline
\quad - Logic & 0.013 & (21\% of Dynamic) \\ \hline
\quad - BRAM & 0.010 & (17\% of Dynamic) \\ \hline
\end{tabular}
\end{table}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{images/Conv_Power.png}
    \caption{Convolution Power Consumption Overview}
\end{figure}

\textbf{Analysis:}
\begin{itemize}
    \item \textbf{Low Power Profile:} The total consumption of $0.164\text{W}$ is minimal, making the IP Core suitable for power-constrained embedded applications.
    \item \textbf{Dominant Static Power:} Static power accounts for 63\% of the total, which is inherent to the FPGA fabric. The Dynamic power is remarkably low ($0.06\text{W}$), reflecting efficient clock gating and localized switching activity within the pipeline.
    \item \textbf{Thermal Stability:} The junction temperature is estimated at $26.9^\circ\text{C}$, providing a safety margin of $73.1^\circ\text{C}$. No active cooling solution is required.
\end{itemize}

\section{Simulation and Verification}

To ensure the correctness of the hardware implementation, a rigorous verification process was conducted using a dual-environment approach: a standard \textbf{Verilog HDL} Testbench for hardware simulation and a Python script acting as the Golden Model for algorithmic validation.

\subsection{Testbench Environment Setup}
The verification environment is designed to simulate real-world data processing using standard Verilog I/O system tasks. The Testbench performs the following operations:
\begin{enumerate}
    \item \textbf{Data Injection:} Reads input vectors from an external text file (\texttt{input\_data.data}) using the \texttt{\$readmemh} system task. The test dataset consists of \textbf{two independent random sequences}, each containing \textbf{128 Floating-Point samples} ($N=128$). The data is stored in 32-bit hexadecimal format to match the hardware memory interface.
    \item \textbf{DUT Stimulation:} Drives the control signals (\texttt{start}, \texttt{rst\_n}) and feeds the random input sequences into the Convolution Core via the AXI-Stream interface protocol.
    \item \textbf{Response Capture:} Monitors the output port and writes the resulting convolution values to a log file (\texttt{output\_result.txt}) using \texttt{\$fwrite} for post-processing analysis.
\end{enumerate}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{images/Input_simu.png}
    \caption{Input Data File containing 128-point Random FP Sequences (Hex Format)}
    \label{fig:input_data}
\end{figure}

\subsection{Simulation Waveforms}
The design was simulated using the Vivado Simulator with the 128-point datasets. Figure \ref{fig:waveform} illustrates the timing diagram of the processing core. 
\begin{itemize}
    \item The \texttt{start} signal triggers the Finite State Machine.
    \item The two 128-point sequences are loaded sequentially into the internal RAMs.
    \item The \texttt{done} signal is asserted high upon completion, indicating that the valid convolution results are available on the output bus.
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=1.0\textwidth]{images/wave_simu.png}
    \caption{Simulation Waveform showing Input Loading and Output Generation}
    \label{fig:waveform}
\end{figure}

The raw output results captured from the simulation are stored in text format as shown below:

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{images/Output_simu.png}
    \caption{Output Result File generated by the Testbench}
    \label{fig:output_data}
\end{figure}

\subsection{Cross-Verification with Python Golden Model}
To validate the numerical accuracy of the hardware, a Python script was developed to calculate the reference convolution using standard scientific libraries (\texttt{numpy}). The script performs two key functions:
\begin{enumerate}
    \item Generates the theoretical circular convolution result for the same two 128-point random input sequences.
    \item Parses the hardware output file (\texttt{output\_result.txt}), converts the IEEE 754 Hex values to Float, and compares them against the theoretical result.
\end{enumerate}

\textbf{Verification Result:}
The automated comparison confirms that the hardware output matches the expected logic for the $N=128$ test case.
\begin{itemize}
    \item \textbf{Status:} PASSED (Hardware output matches Circular Convolution).
    \item \textbf{Precision Error:} The maximum absolute error observed is \textbf{0.000872}.
\end{itemize}

This minor discrepancy is attributed to the inherent precision differences between the hardware's 32-bit Single-Precision Floating Point accumulation and Python's 64-bit Double-Precision calculations. The error falls within the acceptable range for single-precision DSP applications.

\begin{figure}[H]
    \centering
    \includegraphics[width=1.0\textwidth]{images/python_simu.png}
    \caption{Python Verification Script Result: Validating Hardware Accuracy}
    \label{fig:python_verify}
\end{figure}
