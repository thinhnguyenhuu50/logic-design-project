\section{Implementation}

\subsection{Implementation of the RAM}

\subsubsection{Interface}
The module operates on a single clock signal (\texttt{clk}). The interface definition is detailed in Table \ref{tab:interface}. The figure \ref{fig:ram_dual_port} illustrates the block schematic of the True Dual-Port RAM.

\begin{figure}
    \centering
    \includegraphics[width=0.7\textwidth]{images/RAM_schematic.pdf}
    \caption{True Dual-Port RAM Block Schematic}
    \label{fig:ram_dual_port}
\end{figure}

\begin{table}
    \centering
    \caption{RAM Interface Signals}
    \label{tab:interface}
    \begin{tabular}{@{}llll@{}}
        \toprule
        \textbf{Port Name} & \textbf{Direction} & \textbf{Width} & \textbf{Description} \\ 
        \midrule
        \texttt{clk} & Input & 1 & System clock (rising edge triggered). \\
        \midrule
        \multicolumn{4}{l}{\textit{Port A}} \\
        \texttt{data\_a} & Input & \texttt{DATA\_WIDTH} & Data to be written to memory. \\
        \texttt{addr\_a} & Input & \texttt{ADDR\_WIDTH} & Address for read/write operations. \\
        \texttt{we\_a} & Input & 1 & Write Enable (High = Write, Low = Read). \\
        \texttt{q\_a} & Output & \texttt{DATA\_WIDTH} & Data read from memory. \\
        \midrule
        \multicolumn{4}{l}{\textit{Port B}} \\
        \texttt{data\_b} & Input & \texttt{DATA\_WIDTH} & Data to be written to memory. \\
        \texttt{addr\_b} & Input & \texttt{ADDR\_WIDTH} & Address for read/write operations. \\
        \texttt{we\_b} & Input & 1 & Write Enable (High = Write, Low = Read). \\
        \texttt{q\_b} & Output & \texttt{DATA\_WIDTH} & Data read from memory. \\
        \bottomrule
    \end{tabular}
\end{table}

\subsubsection{Functional Behavior}
The RAM operates under the following conditions:
\begin{itemize}
    \item \textbf{Synchronous Operation:} All memory transactions occur on the rising edge of \texttt{clk}.
    \item \textbf{Dual Access:} Port A and Port B operate independently and can be active simultaneously.
    \item \textbf{Read/Write Modes:}
    \begin{itemize}
        \item \textbf{Write Mode (\texttt{we = 1}):} The input data is written to the specified address.
        \item \textbf{Read Mode (\texttt{we = 0}):} The data at the specified address is registered into the output \texttt{q}.
    \end{itemize}
\end{itemize}
However, simultaneous read and write operations to the same address from different ports will lead to undefined behavior.

\subsubsection{Storage Structure}
The memory core is modeled abstractly as a two-dimensional array of registers. This structure is sized dynamically based on the \texttt{DATA\_WIDTH} and \texttt{MEM\_DEPTH} parameters. The model is specifically designed to allow synthesis tools to infer dedicated hardware Block RAM (BRAM) resources on FPGA targets, ensuring optimal area usage and timing performance compared to distributed logic storage.

\subsubsection{Parallel Control Logic}
To achieve True Dual-Port functionality, the design employs two distinct, parallel control processes triggered by the rising edge of the clock. Each process manages the operations of a single port (A or B) independently. Since both processes share access to the same underlying storage array, the component supports simultaneous Read/Read, Read/Write, and Write/Write operations across the two ports.

\subsubsection{Read/Write Arbitration Logic}
The internal logic for each port enforces a mutually exclusive operation mode based on the Write Enable (\texttt{we}) signal:

\begin{enumerate}
    \item \textbf{Write Priority:} When the write enable signal is active, the system prioritizes the storage operation. The data present on the input bus is written directly into the memory array at the specified address.
    
    \item \textbf{Output Behavior during Write:} The implementation utilizes a ``Read-Disabled'' (or No-Change) architecture during write cycles. This means that while data is being written to memory, the output data port is \textit{not} updated with the new input nor the old memory content. Instead, it retains the value held from the previous clock cycle.
    
    \item \textbf{Read Operation:} Only when the write enable signal is inactive does the system retrieve data from the memory array and update the output register.
\end{enumerate}

This logic structure minimizes the hardware overhead by avoiding complex ``Read-First'' or ``Write-First'' arbitration circuitry, making it highly efficient for standard buffer and storage applications.

\subsection{Twiddle Factor ROM Design and Implementation}
\begin{figure}
    \centering
    \includegraphics[width=0.7\textwidth]{images/rom_twiddle_arch.png}
    \caption{The architecture of ROM twiddle factor}
    \label{fig:rom_twiddle_arch}
\end{figure}


The twiddle factor ROM is designed to efficiently generate complex exponential coefficients required by FFT butterfly operations while minimizing memory usage.
Instead of storing the full set of $N$ twiddle factors, the proposed architecture stores sine and cosine values only for the first octant of the unit circle, corresponding to the angular range $[0,\pi/4)$.
All remaining twiddle factors are reconstructed using symmetry properties of trigonometric functions.

The twiddle factor is defined as
\begin{equation}
W_N^k = e^{-j\frac{2\pi k}{N}} = \cos(\beta) - j\sin(\beta),
\quad \beta = \frac{2\pi k}{N}.
\end{equation}

By exploiting symmetry, only $N/8$ base values are stored in ROM, leading to an eightfold reduction in memory size.
The complete twiddle factor reconstruction is achieved using address mirroring, sine--cosine swapping, and sign inversion.

\subsubsection{Symmetrical Mapping of Sine and Cosine}

Table~\ref{tab:twiddle_mapping} summarizes the symmetry rules used to reconstruct sine and cosine values over the full range $[0,2\pi)$ from the first-octant base interval.

\begin{table}[htbp]
\centering
\caption{Symmetrical mapping of twiddle factors}
\label{tab:twiddle_mapping}
\begin{tabular}{c|c|c}
\toprule
Angle range $\beta$ & $\sin(\beta)$ & $\cos(\beta)$ \\
\midrule
$0 \sim \pi/4$ &
$-\sin(\beta)$ &
$\cos(\beta)$ \\

$\pi/4 \sim \pi/2$ &
$-\cos(\tfrac{\pi}{2}-\beta)$ &
$\sin(\tfrac{\pi}{2}-\beta)$ \\

$\pi/2 \sim 3\pi/4$ &
$-\cos(\beta-\tfrac{\pi}{2})$ &
$-\sin(\beta-\tfrac{\pi}{2})$ \\

$3\pi/4 \sim \pi$ &
$-\sin(\pi-\beta)$ &
$-\cos(\pi-\beta)$ \\

$\pi \sim 5\pi/4$ &
$\sin(\beta-\pi)$ &
$-\cos(\beta-\pi)$ \\

$5\pi/4 \sim 3\pi/2$ &
$\cos(\tfrac{3\pi}{2}-\beta)$ &
$-\sin(\tfrac{3\pi}{2}-\beta)$ \\

$3\pi/2 \sim 7\pi/4$ &
$\cos(\beta-\tfrac{3\pi}{2})$ &
$\sin(\beta-\tfrac{3\pi}{2})$ \\

$7\pi/4 \sim 2\pi$ &
$\sin(2\pi-\beta)$ &
$\cos(2\pi-\beta)$ \\
\bottomrule
\end{tabular}
\end{table}

The mapping rules in Table~\ref{tab:twiddle_mapping} form the theoretical basis of the ROM reconstruction logic and are directly reflected in the Verilog implementation.

\subsubsection{Module-Level Implementation Overview}

Fig.~\ref{fig:rom_twiddle_arch} illustrates the structural organization of the twiddle factor generator.
The design is composed of several lightweight combinational modules, each responsible for a specific reconstruction step:

\begin{itemize}
    \item \textbf{\texttt{mux\_addr} (Address Mapping Unit)}  
    This module maps the intra-octant offset to a valid ROM address.
    For odd octants, address mirroring is applied using
    \[
    \texttt{rom\_addr} = (N/8) - \texttt{off},
    \]
    ensuring that all accesses fall within the first-octant ROM range.
    This block is purely combinational and introduces no clock latency.

    \item \textbf{\texttt{rom\_twiddle} (Compact Sine/Cosine ROM)}  
    This module stores precomputed sine and cosine values for angles in $[0,\pi/4)$.
    The ROM is implemented as a combinational lookup table initialized from external data files.
    Once the address is stable, the corresponding sine and cosine values propagate directly to the output.

    \item \textbf{\texttt{Inv\_FP} (Sign Inversion Units)}  
    These modules generate negated sine and cosine values by flipping the IEEE-754 sign bit.
    This method avoids arithmetic subtraction and significantly reduces hardware complexity.
    The sign inversion logic is fully combinational.

    \item \textbf{\texttt{mux\_output} (Symmetry Reconstruction Unit)}  
    Based on the octant index, this module selects between sine and cosine base values,
    applies optional sign inversion, and performs sine--cosine swapping.
    The implemented selection logic directly follows the symmetry rules listed in
    Table~\ref{tab:twiddle_mapping}.

    \item \textbf{\texttt{rom\_twiddle\_top} (Top-Level Twiddle Generator)}  
    This top-level module integrates all submodules and produces the final complex twiddle factor.
    Boundary cases, where the offset equals zero, are handled using predefined constants
    (e.g., $\pm1$, $0$, $\pm\sqrt{2}/2$) to avoid unnecessary ROM access and improve numerical robustness.
\end{itemize}

Although all reconstruction logic is combinational, the generated twiddle factor is synchronized at the system level.
This ensures deterministic timing behavior and seamless integration with the pipelined FFT butterfly units.

Overall, the proposed ROM design achieves substantial memory reduction while maintaining exact trigonometric correctness,
making it well suited for high-performance FFT hardware implementations.

\subsection{Arithmetic Logic Unit (ALU)}

The Arithmetic Logic Unit (ALU) serves as the computational core of the system, designed to execute floating-point (FP) arithmetic operations with high throughput. The architecture is fully pipelined to optimize processing speed while maintaining a compact hardware footprint. The ALU design addresses three distinct operational scenarios: 
\begin{enumerate}
    \item Radix-2 Decimation-in-Frequency (DIF) Butterfly operation.
    \item Radix-2 Decimation-in-Time (DIT) Butterfly operation.
    \item Point-wise Multiplication (MPW) for spectral convolution.
\end{enumerate}

\begin{figure}
    \centering
    \includegraphics[width=0.9\textwidth]{images/ALU_Spec.png}
    \caption{Block Diagram of Arithmetic Logic Unit (ALU)}
\end{figure}

\subsubsection*{Data Path Control and Temporal Alignment}
To support the multi-mode functionality, the datapath employs a network of 64-bit multiplexers (MUX) to dynamically route operands based on the selected operation mode (controlled by signals \texttt{MUX\_BMU} and \texttt{MUX\_MPW}). A critical challenge in pipelined complex arithmetic is ensuring that operands arrive at the arithmetic units (Adders/Multipliers) at the exact same clock cycle. 

To address this, we integrated specific delay buffers (as illustrated in the block diagram) to achieve \textbf{temporal alignment}. These buffers compensate for the latency differences between parallel paths, ensuring the correctness of the algorithm and the integrity of the pipeline stages.

\subsubsection*{Addition Floating Point Unit}

The proposed Floating-Point Adder architecture is compliant with the IEEE 754 single-precision standard and is implemented using a 5-stage pipeline to maximize throughput. The datapath is decomposed into discrete functional blocks, utilizing Sign-Magnitude representation for efficient internal processing. The operation of each pipeline stage is described as follows:

\begin{itemize}
    \item \textbf{Stage 1: Exponent Comparison (\texttt{AFP\_Compare}):} 
    This stage compares the exponents of the two operands ($E_A, E_B$). The absolute difference $\delta = |E_A - E_B|$ is calculated to determine the alignment shift amount. Simultaneously, the larger operand is identified to guide the datapath routing, and the hidden bit is appended to form 24-bit significands.

    \item \textbf{Stage 2: Mantissa Alignment (\texttt{AFP\_Shift}):} 
    To enable arithmetic operations, the significand of the smaller number is right-shifted by $\delta$ positions. This is implemented using a hardware \textit{Barrel Shifter}, ensuring that the binary points of both operands are aligned within a single clock cycle.

    \item \textbf{Stage 3: Arithmetic Operation (\texttt{AFP\_Add}):} 
    The core addition or subtraction is performed based on the effective operation sign. The ALU computes $M_{sum} = M_{large} \pm M_{shifted}$. The result is then converted back to Sign-Magnitude format (taking the absolute value if the result is negative) to prepare for normalization.

    \item \textbf{Stage 4: Normalization Detection (\texttt{AFP\_Normal}):} 
    To handle potential leading zeros resulting from subtraction, this stage employs a \textit{Priority Encoder} (Leading One Detector). It scans the arithmetic result to determine the necessary left-shift amount ($L_{shift}$) required to restore the significand to the normalized $1.xxxx$ format.

    \item \textbf{Stage 5: Normalization and Rounding (\texttt{AFP\_Rounding}):} 
    The final stage executes the normalization shift ($M_{final} = M_{sum} \ll L_{shift}$) and adjusts the exponent accordingly ($E_{final} = E_{large} - L_{shift}$). Finally, Round-to-Nearest logic is applied to the guard bits, and the data is packed into the 32-bit IEEE 754 output format.
\end{itemize}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\textwidth]{images/AddFPSpec.png}
    \caption{5-Stage Pipelined Architecture of the Floating Point Adder}
    \label{fig:fp_adder_arch}
\end{figure}

\subsubsection*{Multiplication Floating Point Unit}


\subsubsection*{Complex Adder Unit}

\subsubsection*{Complex Multiplier Unit}

The design was synthesized and simulated using the Vivado Design Suite with a target clock period of $8\text{ns}$. The latency characterization of the floating-point cores is as follows:
\begin{itemize}
    \item \textbf{FP Adder:} 5 clock cycles.
    \item \textbf{FP Multiplier:} 7 clock cycles.
\end{itemize}

 regarding the complex multiplier implementation, we evaluated the Gauss multiplication algorithm (which uses 3 real multipliers) against the standard method (which uses 4 real multipliers). Although the Gauss method reduces the number of multipliers, our analysis indicated that the additional pre-adders and post-adders required introduced significant structural complexity and pipeline overhead, which negated the area savings in this specific architecture. Consequently, we opted for the \textbf{standard four-multiplication architecture} to maintain pipeline regularity and control simplicity without compromising the overall system performance.

\subsubsection*{Divider N Unit}

\subsection{Control Unit}

\subsection{Convolution Module}